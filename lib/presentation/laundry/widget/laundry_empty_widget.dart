import 'dart:ffi';

import 'package:flutter/cupertino.dart';

class LaundryEmptyWidget extends StatelessWidget {
  const LaundryEmptyWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 82,
      width: 148,
    );
  }
}
